

function setRadio(radio) {
// función some hace una función hasta que la función retorne un true, no da más vueltas y no lo repita más veces
    radio.some()

    let value
    for (let i = 0; i < radiolength; i++) {
        const item = radio[i];
        it (item.checked) {
            return item.value
        }
    }
    return 
            }
        }
    )
}